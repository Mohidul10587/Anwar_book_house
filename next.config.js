/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "i.ibb.co", "test.notebookprokash.com"],
  },
};

module.exports = nextConfig;
